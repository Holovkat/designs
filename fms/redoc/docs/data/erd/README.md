---
last-redoc-date: 2025-11-12T23:25:18Z
---
# ERD Assets (Placeholder)

The source tree reserves `docs/data/erd/` for rendered ERD exports (SVG/PNG) or supplemental diagram assets that complement `docs/data/erd.md`. At present the folder is empty; Mermaid diagrams embedded in `erd.md` already cover Convex and Postgres relationships.

When future iterations add image exports or tool-specific files, store them here so the ReDoc reverse-tree process can surface them without bloating the narrative ERD document.

