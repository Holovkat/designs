# Infrastructure-only env (do not add app config here)
# These values exist solely to start containers locally. All application
# configuration must be persisted in the database and edited at runtime.

# Core infra
POSTGRES_USER=fms
POSTGRES_PASSWORD=fms
POSTGRES_DB=fms
POSTGRES_PORT=5432

REDIS_PORT=6379

N8N_PORT=5678
N8N_HOST=localhost
N8N_ENCRYPTION_KEY=change_me_in_prod
N8N_WORKER_CONCURRENCY=5
WEBHOOK_URL=http://localhost:5678/

# Events
KAFKA_UI_PORT=8080

# Aux tooling
LOCALSTACK_PORT=4566
OSRM_PORT=5000
